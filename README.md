# Fintrack Frontend

## Local run
```bash
cd /Users/sampoelmans/Documents/SamITSolutions/financesp/finance-frontend
npm install
npm start
```

Backend default is `http://localhost:8085` configured in `src/environments/environment.ts`.
# fintrack-frontend
